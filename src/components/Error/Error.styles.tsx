import styled from 'styled-components'

export const StyledError = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rem;
  padding: 5rem 0;
  color: var(--white);
  font-size: 1.25rem;
  font-weight: 700;

  > svg {
    height: 5rem;
  }
`

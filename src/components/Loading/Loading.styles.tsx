import styled from 'styled-components'

export const StyledLoading = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);

  svg {
    width: 4rem;
  }

  p {
    font-size: 1rem;
    text-align: center;
    font-size: 1rem;
    color: #fff;
  }
`

import styled from 'styled-components'

export const StyledProducts = styled.div`
  padding: 2rem 6.25rem 4rem;
  display: flex;

  & > main {
    display: flex;
    flex-wrap: wrap;
    gap: 2.5rem;
    min-width: 32rem;
  }
`
